execution_env =local    
//execution_env=Remote                         
                         /*
                          *by default  if we make it as a local all ets cases will be executed on local 
                          *if make it as a Remote all test cases will be executed on the Remote always becuase every time we dont  run on grid environment 
                          *suppose if using second type environment so this concept or if we using vms or if we using Docker concept still our code will not be changed  same code will be used we dont need to change any code in the base class
                          */
appURL1=http://localhost/opencart/upload/
appURL2=http://tutorialsninja.com/demo/
email=pavanoltraining@gmail.com
password=test@123
searchProductName=iphone
